name: Secret scan (self)

# shared-resources scans ITSELF with the reusable workflow it hosts (dog-fooding). Same-repo call
# uses the local ref so the config is found at the repo root (shared-configs/gitleaks/).
on:
  push:
  pull_request:

jobs:
  secret-scan:
    uses: ./.github/workflows/secret-scan.yml
